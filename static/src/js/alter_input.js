odoo.define('hr_china.alter_input', function (require) {
    "use strict";

    var core = require('web.core');
    var utils = require('web.utils');
    var Model = require('web.Model');
    var FormRenderer = require('web.FormRenderer');


    FormRenderer.include({
        var self = this

        console.log(self)
    })
});